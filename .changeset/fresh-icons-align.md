---
"grex": patch
---

Replace the leftover legacy app icon and browser favicon with the current Grex hexagon mark, and rebuild the macOS icon set with the standard icon-grid padding so the Dock icon no longer renders oversized next to other apps.
