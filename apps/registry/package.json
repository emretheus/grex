{
	"name": "@codewit/registry",
	"description": "Codewit companion device registry — writes remote-*.codewit.ai CNAMEs for paired desktops.",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"test": "bun test",
		"typecheck": "tsc --noEmit",
		"dev": "wrangler dev",
		"deploy": "wrangler deploy"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20250620.0",
		"typescript": "6.0.2",
		"wrangler": "^4.20.0"
	}
}
