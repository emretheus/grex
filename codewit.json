{
	"scripts": {
		"setup": "cp \"$CODEWIT_ROOT_PATH/.env.local\" \"$CODEWIT_WORKSPACE_PATH/.env.local\"\ncp -r \"$CODEWIT_ROOT_PATH/sidecar/dist\" \"$CODEWIT_WORKSPACE_PATH/sidecar/dist\"\nbun install",
		"run": [
			{
				"name": "Default",
				"command": "bun run dev",
				"stopCommand": "",
				"mode": "non-concurrent"
			}
		]
	}
}
